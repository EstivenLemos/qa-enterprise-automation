spring.application.name=smartstore

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:smartstore}
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:admin123}

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

server.port=8080

jwt.secret=${JWT_SECRET:CAMBIAR_POR_UNA_LLAVE_MUY_LARGA_DE_AL_MENOS_32_BYTES_PARA_HS256}

jwt.expiration=900000

jwt.refresh-expiration=604800000